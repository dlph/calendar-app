local_resource(
    'ui:dev',
    cmd='',
    serve_cmd='yarn dev',
)

local_resource(
    'cargo build',
    dir='src-tauri',
    cmd='cargo build',
    auto_init=False,
)

local_resource(
    'tauri build',
    cmd='cargo tauri build',
)

local_resource(
    'yarn',
    cmd='yarn install',
    auto_init=False,
)
